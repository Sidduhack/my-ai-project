# Frontend Environment Variables
# Copy to frontend/.env.local and update with your backend URL

# Backend API URL (deployed separately on Railway/Render/Fly.io)
VITE_API_URL=https://YOUR_BACKEND_URL

# WebSocket URL for real-time updates
VITE_WS_URL=wss://YOUR_BACKEND_URL

# Optional: Analytics
VITE_GA_ID=